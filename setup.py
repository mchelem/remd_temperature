from distutils.core import setup
setup(
    name='remd_temperature',
    version='1.0',
    py_modules=['remd_temperature'],
    install_requires=[
        'requests',
        'beautifulsoup'
    ]
)
